jdk:
  - openjdk17

before_install:
  - chmod +x gradlew

install:
  - ./gradlew :saga-core:publishToMavenLocal :saga-storage-jdbc:publishToMavenLocal :saga-spring-boot-starter:publishToMavenLocal -x test
